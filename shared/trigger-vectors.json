{
  "_about": "Conformance vectors for external trigger wire parsing and token resolution. The contract: every implementation (JS web player, Kotlin/Android, and any future one) must agree with every vector. If an implementation disagrees, the implementation is wrong. See docs/triggers-design.md.",
  "_why_this_file_exists": "The fire path decides whether an unauthenticated packet arriving from a LAN changes what is on a screen. Two implementations of that in two languages will drift, and the drift will be silent and security-relevant — one player accepting a payload another refuses is a hole nobody sees until it is used. Vectors make the contract the artifact and the implementations the thing being checked.",
  "_conventions": {
    "wire": "ST1 <secret> <token> — exactly three space-separated fields on one line. A trailing \\n or \\r\\n is tolerated because most integrator gear appends one.",
    "magic": "The literal 'ST1'. Checked before any splitting: on subnet broadcast the socket sees every stray datagram on the LAN, so rejection has to be cheap and bad_magic has to stay meaningful as 'the network is noisy' rather than 'someone is getting it wrong'.",
    "token_charset": "Printable ASCII 0x21-0x7E, 1-64 chars. No spaces — the payload is space-separated, so a token containing one cannot survive the wire. Refused at save time too, in routes/triggers.js.",
    "max_bytes": "512. Bounds the work one datagram can cause.",
    "secret": "Compared for equality against the per-device shared secret. An absent or empty device secret refuses everything, so an unprovisioned player is inert rather than open.",
    "source": "'http' or 'udp'. A trigger that does not accept the arriving transport must not match — enabling only UDP on an alarm says it is fired by the panel wired to it, not by anything that can reach the box over HTTP.",
    "scope": "The trigger list is already device-scoped by the server at sync time. A token that is real on another screen is simply unknown here.",
    "reasons": "bad_magic | malformed | too_large | bad_secret | unknown_token. Closed set: these are the counters an installer reads, and 'rejected: 41' without a breakdown answers nothing.",
    "precedence": "clear_all is checked before per-trigger tokens, so a device-level stop cannot be shadowed by a trigger that happens to use the same token."
  },
  "device_secret": "s3cret-s3cret-s3",
  "clear_all_token": "ALLSTOP",
  "triggers": [
    { "id": "t1", "name": "Evacuate", "match_token": "EVAC", "clear_token": "EVAC_CLR",
      "source_http": true, "source_udp": true, "mode": "until_cleared", "priority": 100 },
    { "id": "t2", "name": "UDP only", "match_token": "UDPONLY", "clear_token": null,
      "source_http": false, "source_udp": true, "mode": "once", "priority": 10 },
    { "id": "t3", "name": "HTTP only", "match_token": "HTTPONLY", "clear_token": null,
      "source_http": true, "source_udp": false, "mode": "once", "priority": 10 }
  ],
  "vectors": [
    { "description": "a well-formed fire over udp",
      "text": "ST1 s3cret-s3cret-s3 EVAC", "source": "udp", "expect": { "ok": true, "action": "fire", "trigger_id": "t1" } },
    { "description": "the same fire over http",
      "text": "ST1 s3cret-s3cret-s3 EVAC", "source": "http", "expect": { "ok": true, "action": "fire", "trigger_id": "t1" } },
    { "description": "a clear token resolves to clear on its own trigger",
      "text": "ST1 s3cret-s3cret-s3 EVAC_CLR", "source": "udp", "expect": { "ok": true, "action": "clear", "trigger_id": "t1" } },
    { "description": "the device-level clear-all names no trigger",
      "text": "ST1 s3cret-s3cret-s3 ALLSTOP", "source": "udp", "expect": { "ok": true, "action": "clear_all", "trigger_id": null } },

    { "description": "a trailing newline is tolerated — most gear appends one",
      "text": "ST1 s3cret-s3cret-s3 EVAC\n", "source": "udp", "expect": { "ok": true, "action": "fire", "trigger_id": "t1" } },
    { "description": "a trailing CRLF is tolerated too",
      "text": "ST1 s3cret-s3cret-s3 EVAC\r\n", "source": "udp", "expect": { "ok": true, "action": "fire", "trigger_id": "t1" } },

    { "description": "a UDP-only trigger must NOT fire over http",
      "text": "ST1 s3cret-s3cret-s3 UDPONLY", "source": "http", "expect": { "ok": false, "reason": "unknown_token" } },
    { "description": "...and does fire over udp",
      "text": "ST1 s3cret-s3cret-s3 UDPONLY", "source": "udp", "expect": { "ok": true, "action": "fire", "trigger_id": "t2" } },
    { "description": "an HTTP-only trigger must NOT fire over udp",
      "text": "ST1 s3cret-s3cret-s3 HTTPONLY", "source": "udp", "expect": { "ok": false, "reason": "unknown_token" } },
    { "description": "...and does fire over http",
      "text": "ST1 s3cret-s3cret-s3 HTTPONLY", "source": "http", "expect": { "ok": true, "action": "fire", "trigger_id": "t3" } },

    { "description": "the wrong secret is refused",
      "text": "ST1 wrong-wrong-wrong EVAC", "source": "udp", "expect": { "ok": false, "reason": "bad_secret" } },
    { "description": "a secret of the right length but wrong content is still refused",
      "text": "ST1 s3cret-s3cret-sX EVAC", "source": "udp", "expect": { "ok": false, "reason": "bad_secret" } },
    { "description": "a token valid on another screen is simply unknown here",
      "text": "ST1 s3cret-s3cret-s3 SOMEONE_ELSES", "source": "udp", "expect": { "ok": false, "reason": "unknown_token" } },

    { "description": "broadcast noise: SSDP",
      "text": "M-SEARCH * HTTP/1.1", "source": "udp", "expect": { "ok": false, "reason": "bad_magic" } },
    { "description": "broadcast noise: JSON from something else entirely",
      "text": "{\"jsonrpc\":\"2.0\"}", "source": "udp", "expect": { "ok": false, "reason": "bad_magic" } },
    { "description": "an empty datagram",
      "text": "", "source": "udp", "expect": { "ok": false, "reason": "bad_magic" } },
    { "description": "the magic alone, nothing else",
      "text": "ST1", "source": "udp", "expect": { "ok": false, "reason": "malformed" } },
    { "description": "a near-miss magic is noise, not malformed",
      "text": "ST2 s3cret-s3cret-s3 EVAC", "source": "udp", "expect": { "ok": false, "reason": "bad_magic" } },

    { "description": "a missing secret field is malformed, not bad_secret — the sender's FORMAT is wrong",
      "text": "ST1 EVAC", "source": "udp", "expect": { "ok": false, "reason": "malformed" } },
    { "description": "a token containing a space cannot be represented on the wire",
      "text": "ST1 s3cret-s3cret-s3 FIRE ALARM", "source": "udp", "expect": { "ok": false, "reason": "malformed" } },
    { "description": "an empty token",
      "text": "ST1 s3cret-s3cret-s3 ", "source": "udp", "expect": { "ok": false, "reason": "malformed" } },
    { "description": "a token with a control character is not printable ASCII",
      "text": "ST1 s3cret-s3cret-s3 EV\u0001AC", "source": "udp", "expect": { "ok": false, "reason": "malformed" } },

    { "description": "over the 512-byte cap",
      "text": "ST1 s3cret-s3cret-s3 ",
      "source": "udp", "expect": { "ok": false, "reason": "too_large" } },

    { "description": "an unprovisioned device (no secret) refuses everything",
      "device_secret": "", "text": "ST1 s3cret-s3cret-s3 EVAC", "source": "udp", "expect": { "ok": false, "reason": "bad_secret" } },
    { "description": "with no clear-all configured, that token is just unknown",
      "clear_all_token": null, "text": "ST1 s3cret-s3cret-s3 ALLSTOP", "source": "udp", "expect": { "ok": false, "reason": "unknown_token" } },
    { "description": "a device holding no triggers refuses a real token",
      "triggers": [], "text": "ST1 s3cret-s3cret-s3 EVAC", "source": "udp", "expect": { "ok": false, "reason": "unknown_token" } },
    { "description": "clear_all takes precedence over a trigger using the same token",
      "triggers": [{ "id": "tX", "name": "Clash", "match_token": "ALLSTOP", "clear_token": null,
                     "source_http": true, "source_udp": true, "mode": "once", "priority": 0 }],
      "text": "ST1 s3cret-s3cret-s3 ALLSTOP", "source": "udp", "expect": { "ok": true, "action": "clear_all", "trigger_id": null } }
  ]
}
